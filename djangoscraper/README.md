# DjangoScraper

Working out how to get through CSRF to log into a django app

## Setup

    python3 -m venv venv
    . venv/bin/activate
    pip install -r requirements.txt

Then

    cp env.template .env

and fill out `.env`
